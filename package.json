{
  "name": "mapkit",
  "version": "0.0.0",
  "private": true,
  "description": "A React and Next.js map UI kit with adapter support for Leaflet, MapLibre, Google Maps, and Mapbox.",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
