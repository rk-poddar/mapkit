{
  "name": "@map-kit/draw",
  "version": "0.0.0",
  "private": true,
  "description": "Drawing and geofence utilities for Map Kit.",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for @map-kit/draw yet\"",
    "test": "echo \"No tests configured for @map-kit/draw yet\""
  },
  "dependencies": {
    "@map-kit/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
